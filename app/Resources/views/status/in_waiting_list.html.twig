{% if user.main_club.status == 'lottery_closed' %}
    <p class="lead">
        Tu es dans notre liste d'attente. Tu recevras un mail quand les inscriptions ouvriront.
    </p>
{% else %}
    <p>Le tirage au sort est ouvert !</p>
    <a href="{{ path('request_lottery_participation') }}">
        <button class="btn btn-primary btn-lg">Participer</button>
    </a>
{% endif %}
